import ISoundFile from '../../models/sound-file';

const soundFiles: Record<string, ISoundFile> = {
    windchimes: {
        name: 'Wind Chimes',
        filename: '118979__esperri__windchimes-2.wav',
    },
};

export default soundFiles;
